---
- name: remove old docker packages
  yum:
    name:
      - docker
      - docker-client
      - docker-client-latest
      - docker-common
      - docker-latest
      - docker-latest-logrotate
      - docker-logrotate
      - docker-engine
    state: absent

- name: install yum-utils
  yum:
    name: yum-utils
    state: present

- name: add docker repository
  shell: yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

- name: install docker packages
  yum:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
    state: present
    update_cache: yes

- name: install docker dsk for python
  pip:
    name: docker
    state: latest

- name: start docker service
  service:
    name: docker
    enabled: yes
    state: started

- name: test
  docker_container:
    image: alpine
    name: yolo

- name: test
  docker_container:
    name: yolo
    state: absent
...